[project]
name = "japanese-font-library-fonts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fonttools>=4.61.1",
    "otf2ttf>=0.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
