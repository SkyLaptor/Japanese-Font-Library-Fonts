[project]
name = "japanese-font-library-fonts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fonttools>=4.61.1",
    "gftools>=0.9.99",
    "otf2ttf>=0.2",
    "skia-pathops>=0.9.1",
]

[tool.uv]
package = true

[project.scripts]
font_tools = "utils.font_tools:main"
convert_for_skyrim = "convert_for_skyrim:main"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
